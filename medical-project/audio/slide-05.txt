The application layer mattered too. This was not just prompt wiring. The Flask app wrapped the workflow with document intake, workflow state, review checkpoints, and a deployable surface that could function as a real tool.
