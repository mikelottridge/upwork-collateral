The pipeline keeps source precedence explicit and testable. It ingests companyfacts, filings, exhibits, PDFs, and transcript-shaped sources. It extracts values through tag aliases, document patterns, PDF table logic, and constrained schemas. It normalizes everything to one row shape, then resolves accepted values with precedence, confidence flags, missing states, and conflict handling.
