The proof set is intentionally small, but the configuration, row contract, validation hooks, and bounded PDF path are the same pieces needed for forty to fifty companies. Production hardening focuses on page selection, retry behavior, review thresholds, and transcript coverage. It does not require rebuilding the output contract for every company. Periods, units, source age, and conflicts can all be validated before export.
