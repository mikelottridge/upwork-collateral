Model output is an extraction aid, never the source authority. The app does not call the model on page load or use it to decide source precedence. One difficult PDF page is selected within a recorded token and cost boundary. Blank cells stay blank, and the output keeps the source URL, page number, checksum, excerpt, and confidence flag. The client controls the model, page selection rule, and validation threshold before scale.
